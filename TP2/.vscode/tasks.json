{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make",
            "args": [], // Adicione argumentos aqui se necessário
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
